version: 2
jobs:
  build:
    steps:
      - setup_remote_docker
      - run: 'sudo docker build -t fischcheng/docker-react -f Dockerfile.dev .'
      - run: 'sudo docker run -e CI=true fischcheng/docker-react npm run test'
